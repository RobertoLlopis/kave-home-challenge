export type PriceProps = {
  value: number
  previous?: number | null
  className?: string
}
export type PreviousPriceProps = { value: number }
export type PreviousPartProps = { previous?: number | null; value: number }
